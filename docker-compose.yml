services:
  pxe:
    build: .
    container_name: pxe-server
    restart: unless-stopped
    network_mode: host
    cap_add:
      - NET_ADMIN
    env_file:
      - .env
    volumes:
      - alpine-netboot:/pxe
volumes:
  alpine-netboot:
